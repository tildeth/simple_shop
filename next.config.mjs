/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.dummyjson.com'], // Tilføj domænet, hvor billederne bliver hostet
      },
};

export default nextConfig;
